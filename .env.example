# Node Environment (development | production)
NODE_ENV=development

PORT=3000

# Local MongoDB avec Docker (pour développement)
MONGODB_URI=mongodb://tspark:tspark123@localhost:27017/tspark?authSource=admin

# Production MongoDB Atlas (remplacer <username>, <password>, <cluster> et <dbname>)
# MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority&appName=4IWA-Node-TSPark

# JWT Secret Key (CHANGER EN PRODUCTION)
# Générer une clé sécurisée avec: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
